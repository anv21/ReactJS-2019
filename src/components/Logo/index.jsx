import React from 'react';
import styled from 'styled-components';

const LogoSection = styled.span`
  color: #f55263;
  font-size: 20px;
  font-weight: bold;
  letter-spacing: 0.25px;
`;

const Logo = () => <LogoSection>netflixroullete</LogoSection>;

export default Logo;
